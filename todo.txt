optimizations:
    remove superflous () when generating yolol
    remove static ifs
        (attention with mulitline ifs)
        line numbers!!!
    simplify static expressions
    shorten variable names


nolol features:
    check number constants for precision
    comments:
        introduce no empty line, keep line-numbering intact
    multiline-lines
        \ or {}
    declare external vars
        warn if using undeclared
        alias for external variables
    typecheck + cast
        warn if wrong types
    named constants
    multiline if
        line numbers?
    include other files
        line-number hints for tokenizer
    remove empty lines
        special keyword/character for keeping them?
        replace woth sleep?
        incompatible with yolol?
    builtin functions
        e.g. sleep
    macros:
        c-style or inline functions?
        expression or statement?
    goto-labels
        handle line numbering
        must be first stmt on line
    loops
       
