features:
    automatic testing


nolol features:
    remove static ifs
        (attention with mulitline ifs)
        line numbers!!!
    check number constants for precision
    declare external vars
        warn if using undeclared
        alias for external variables
    typecheck + cast
        warn if wrong types
    include other files
        line-number hints for tokenizer
    introduce sleep()
    macros:
        c-style or inline functions?
        expression or statement?
    continue, break, 
    elseif
       
